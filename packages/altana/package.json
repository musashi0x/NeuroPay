{
  "name": "@neuro-pay/altana",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run",
    "provision": "tsx --env-file-if-exists=.env --env-file-if-exists=../../apps/api/.env scripts/provision.ts",
    "revoke": "tsx --env-file-if-exists=.env --env-file-if-exists=../../apps/api/.env scripts/revoke.ts",
    "probe:fee": "tsx --env-file-if-exists=.env --env-file-if-exists=../../apps/api/.env scripts/first-action-fee.ts",
    "runbook": "tsx --env-file-if-exists=.env --env-file-if-exists=../../apps/api/.env scripts/runbook.ts",
    "test:chain": "vitest run --config vitest.chain.config.ts"
  },
  "dependencies": {
    "@altananetwork/sdk": "catalog:",
    "@neuro-pay/metering": "workspace:*",
    "@neuro-pay/types": "workspace:*",
    "viem": "catalog:"
  },
  "devDependencies": {
    "@neuro-pay/eslint-config": "workspace:*",
    "@neuro-pay/evm-testnet": "workspace:*",
    "@neuro-pay/tsconfig": "workspace:*",
    "@types/node": "^22.18.12",
    "eslint": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
